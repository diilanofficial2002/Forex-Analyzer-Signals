PAIR: {pair}
TIF_DATE: {tif_date}

# Indicators (H1 + spot)
{indicators}

# Recent High-impact News (Bangkok) — keep signal-relevant:
{recent_news}

# Constraints
RR_MIN: {rr_min}
RR_MAX: {rr_max}
PREFER_RR: {prefer_rr}

# Task
Given the above, propose a single OCO plan using ONLY the allowed combos, respecting pending semantics and TP/SL realism vs ATR/ADR and spread.
Return STRICTLY a single JSON object exactly in the schema described by the system message (no extra text).